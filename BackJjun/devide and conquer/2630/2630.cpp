#include <bits/stdc++.h>
using namespace std;

typedef vector<string> intv;

int main(){
    int N,exponent,times;
    cin>>N;
    while(N--){
        intv v;
        cin>>exponent;
        times=pow(3,exponent);
        while(times--){
            v.push_back("-");
        }
        if(v[0]==v[1]){
            
        }










    }
}
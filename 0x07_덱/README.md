#덱 

find를 사용해서 인덱스를 정수화 하자
int idx = find(DQ.begin(), DQ.end(), t) - DQ.begin(); // idx : t가 있는 위치

push_front(),push_back(),size(),empty(),pop_front(),pop_back()
insert(it,value),erase(it,value),clear()
